# server/.env — secrets and per-deployment settings ONLY.
#
# Static portal URLs (login pages, portal endpoints) are NOT here: they are the
# same for every deployment, so they live as defaults in app/config.py and are
# the single source of truth. Put only secrets and things that legitimately
# change per environment below.
#
# QUOTING — wrap any password containing punctuation in SINGLE quotes:
#
#     UNISON_PASSWORD='pa$$w0rd%#with^symbols'
#
# Single quotes are the safest form every reader of this file agrees on, as
# measured against python-dotenv 1.2.2 and `source .env` in bash:
#
#   in the value      unquoted              "double"              'single'
#   ---------------------------------------------------------------------------
#   ` #` (after a     TRUNCATED — the       safe                  safe
#   space)            rest is a comment
#   $NAME             safe in dotenv,       safe in dotenv,       safe
#                     EXPANDED by a shell   EXPANDED by a shell
#   ${NAME}           EXPANDED              EXPANDED              EXPANDED
#   backslash         safe in dotenv,       MANGLED — \b, \t etc  safe
#                     eaten by a shell      are read as escapes
#
# So: use single quotes. The one case no quoting can save is a literal `${` —
# python-dotenv expands it whatever the quoting, so a password containing `${`
# has to be passed as a real environment variable instead of through this file.
# A single quote itself also cannot appear inside single quotes.
#
# app/core/credentials.py checks all of this at run start: it re-reads the raw
# line and compares it against what the app loaded, so a mangled credential is
# reported as a mangled credential rather than as a portal login failure.

# -- Portal credentials ------------------------------------------------------
MFMP_EMAIL=your_email_here
MFMP_PASSWORD=your_password_here

# RideMetro (Bonfire) runs as one of two accounts, chosen per run in the
# console. They are separate logins into separate Euna Supplier Networks, so the
# choice decides which agencies a run sweeps. An account with either key blank
# is shown as unavailable in the picker and cannot be started.
HOOPE_LAB_USERNAME=your_email_here
HOOPE_LAB_PASSWORD=your_password_here

FEDPINTS_USERNAME=your_email_here
FEDPINTS_PASSWORD=your_password_here

# Superseded by HOOPE_LAB_* above, and still read as that account's credentials
# when they are set and HOOPE_LAB_* are not. Leave blank on a new deployment.
# RIDEMETRO_EMAIL=
# RIDEMETRO_PASSWORD=

BIDNET_USERNAME=your_email_here
BIDNET_PASSWORD=your_password_here

NORTHDAKOTA_USERNAME=your_username_here
NORTHDAKOTA_PASSWORD=your_password_here
# Manual-login mode: force a visible browser and wait for a human to solve the
# B2C reCAPTCHA. Requires a desktop/display where someone is present.
NORTHDAKOTA_MANUAL_LOGIN=true
NORTHDAKOTA_MANUAL_LOGIN_TIMEOUT=300

SEPTA_USERNAME=your_username_here
SEPTA_PASSWORD=your_password_here

Cal_ePROCURE_USERNAME=your_username_here
Cal_ePROCURE_PASSWORD=your_password_here

# Unison Marketplace credentials (SAM.gov and NAICS need no credentials;
# Wisconsin is a public portal with no login).
UNISON_EMAIL=your_email@example.com
# Single-quoted: this account's password carries $ / % / # / ^ (see QUOTING above).
UNISON_PASSWORD='your_password'

# -- Post-scrape email / S3 notifications ------------------------------------
# On a successful run the archive ZIP (cumulative Excel + documents) is emailed
# via AWS SES, with the download link in the body and an optional S3 upload.
# Leave RECIPIENT_EMAILS blank to disable notifications entirely.
RECIPIENT_EMAILS=email1@example.com,email2@example.com
AWS_S3_BUCKET_NAME=your_s3_bucket_name
AWS_ACCESS_KEY_ID=your_aws_access_key_id
AWS_SECRET_ACCESS_KEY=your_aws_secret_access_key
AWS_REGION=us-east-1
AWS_SES_FROM_EMAIL=no-reply@yourdomain.com
AWS_SES_USERNAME=your_ses_smtp_username
AWS_SES_PASSWORD=your_ses_smtp_password

# Base URL recipients can reach this server at — used to build the download
# link in the notification email. This is deployment-specific (localhost in dev,
# your public domain/tunnel in production), which is why it belongs here.
PUBLIC_BASE_URL=http://localhost:8000

# -- Storage & runtime (optional — sensible defaults in app/config.py) --------
# SCRAPE_CONCURRENCY  how many scrapes run at once; the rest queue (default 3).
#                     Each run drives its own Chrome at ~300-500 MB, so this is
#                     a memory budget more than a speed dial. Raise on a bigger box.
# WORK_DIR   scratch workspace for in-flight runs (default: system temp dir)
# ARCHIVE_DIR where finished-run ZIPs are stored (default: ../data/archives)
# WORK_DIR=
# ARCHIVE_DIR=../data/archives

# -- Database ----------------------------------------------------------------
DATABASE_URL=postgresql+psycopg2://USER:PASSWORD@localhost:5432/scraping-hub
