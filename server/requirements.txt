fastapi
uvicorn[standard]
selenium
webdriver-manager
python-dotenv
pydantic
pydantic-settings
sqlalchemy
psycopg2-binary
openpyxl
# Legacy .xls only — openpyxl reads .xlsx and cannot open the old binary
# format, which is still what a client's spreadsheet arrives as. Used by
# app/scrapers/naics/importer.py.
xlrd
# Vendored SAM / Unison / NAICS engine dependencies (app/scrapers/*/engine/).
beautifulsoup4
lxml
PyMuPDF
python-docx
pandas
requests
PyYAML
ollama
# Post-scrape S3 upload for the email notifier (SES email itself uses stdlib smtplib).
boto3
